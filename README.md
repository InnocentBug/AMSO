# AMSO: Accelerated Monte-carlo: Soma Optimized | Assembling Macromolecules: Soft Objects

This is a redesign of [SOMA](https://gitlab.com/InnocentBug/SOMA).

## Installation

This is python project that uses C++/CUDA for backend operations.
For you as a user it should feel like a python project and you can install it via pip as follows:

```bash
pip install .
```

THe C++/CUDA side is being built by CMake (and ninja) and you can pass
